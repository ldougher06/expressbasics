<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <h1>My Site Title is <%= title %></h1>
    <h2><%= welcome %></h2>
    <h3>These are the most awesome things...
      <ul>
        <% awesomeThings.forEach(function(thing) { %>
          <li><%= thing %> is awesome</li>
      <% }); %>
      </ul>
    </h3>
  </body>
</html>
